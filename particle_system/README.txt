# OpenGL Particle System

A particle system drawn using OpenGL.
Command line arguments are accepted to customize the particle system.

It also contain a partial implementation of deflectors to reflect the particles, like walls.
